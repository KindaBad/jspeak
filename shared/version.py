"""Single source of truth for the app version and update endpoints."""

__version__ = "0.1.0-beta"

# Public repo that hosts the source and downloadable release binaries (the
# auto-updater checks this repo's latest GitHub release).
RELEASES_REPO = "KindaBad/jspeak"
